apiVersion: v1
kind: Service
metadata:
  namespace: ml-project
  name: frontend-service
  labels:
    name: frontend-service
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - name: http
      port: 80 
      targetPort: 80
      nodePort: 30006
      name: frontend-cluster-ip
